{
  "name": "recipe-parser",
  "version": "1.0.0",
  "description": "Obsidian plugin to pull out recipe information from attachments",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "release": "npm run build && node scripts/release.mjs"
  },
  "author": "Jim Wild",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.4",
    "obsidian": "^1.8.10",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "tslib": "^2.8.1"
  }
}
